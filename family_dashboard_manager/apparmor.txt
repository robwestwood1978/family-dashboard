#include <tunables/global>

profile family_dashboard_manager flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  network inet stream,
  network inet6 stream,
  signal (send) set=(kill,term,int,hup,cont),

  # S6-Overlay. /init is interpreted by the shell and therefore needs read
  # permission as well as execute permission.
  /init rix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** rix,
  /package/** rix,
  /command/** rix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,

  /usr/lib/** mr,
  /lib/** mr,
  /lib64/** mr,
  /dev/null rw,
  /dev/tty rw,
  /dev/urandom r,
  /etc/{,**} r,
  /usr/lib/bashio/** rix,
  /opt/family-dashboard/{,**} rix,
  /tmp/{,**} rwk,
  /data/{,**} rwk,

  /config/family-dashboard/ rw,
  /config/family-dashboard/** rwk,
  /config/www/family-dashboard/ rw,
  /config/www/family-dashboard/** rwk,

  # Only the fixed Classroom integration and its bounded staging/recovery paths
  # are writable. OAuth config entries and tokens remain in Home Assistant's
  # protected .storage directory; this profile grants Manager no access to it.
  /config/custom_components/ rw,
  /config/custom_components/family_dashboard_classroom/ rw,
  /config/custom_components/family_dashboard_classroom/** rwk,
  /config/custom_components/.family_dashboard_classroom-staging/ rw,
  /config/custom_components/.family_dashboard_classroom-staging/** rwk,
  /config/custom_components/.family_dashboard_classroom-previous/ rw,
  /config/custom_components/.family_dashboard_classroom-previous/** rwk,
}
